pipeline {
    agent any
    stages {
        stage('go version') {
            steps {
                sh 'go version'
            }
        }
        stage('build') {
            steps {
                sh 'go '
            }
        }
    }
}